{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {},
    "fix": {},
    "test": {},
    "build": {
      "dependsOn": ["lint", "test"],
      "outputs": ["dist/**"]
    },
    "lint-examples": {
      "dependsOn": ["build"]
    },
    "fix-examples": {
      "dependsOn": ["build"]
    }
  }
}
